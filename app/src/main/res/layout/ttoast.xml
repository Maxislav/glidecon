<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">
    <data>
        <variable
            name="myText"
            type="String" />
        <variable
            name="tToast"
            type="com.atlas.mars.glidecon.model.TToast" />
    </data>
    <LinearLayout
        android:layout_marginTop="50dp"
        android:gravity="center_vertical"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        >
        <TextView

            android:textColor="@{tToast.textColor}"
            android:padding="5dp"
            android:background="@{tToast.background}"
            android:layout_width="wrap_content"
            android:text="@{tToast.message}"
            android:layout_height="wrap_content">
        </TextView>

    </LinearLayout>
</layout>
